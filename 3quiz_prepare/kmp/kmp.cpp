#include <bits/stdc++.h>
using namespace std;

vector <int> computeLPS(const string& pattern){
    int n = pattern.lenght() ;
    vector<int> lps(n , 0) ; 
    int lenght = 0 ;
    int i = 1 ; 
    while(i < n ){
        if(pattern)
    }
}